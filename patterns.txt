(Failed|Invalid|Deny)*(password|user|root)* SSH_BRUTEFORCE high
(SELECT|UNION|INSERT|DELETE)*(FROM|WHERE|SCHEMA)* SQL_INJECTION critical
(script|javascript|onerror)*(alert|eval|cookie)* XSS_ATTACK medium
(bin/sh|bin/bash|cmd.exe)*(wget|curl|chmod)* CMD_INJECTION critical
(Nmap|Nikto|Sqlmap|Gobuster)*(Scan|Probe|Test)* SCANNER_PROBE low
(Kernel|OOM|Panic)*(Segmentation|out_of_memory)* SYSTEM_FAULT medium
